import { User } from '../../../../api/api-types'

const AdminUserDelete = ({ user }: {
  user: User,
}) => {
  user;
  return (
    <div className="admin-single-user-delete">
    
    </div>
  )
}

export default AdminUserDelete
