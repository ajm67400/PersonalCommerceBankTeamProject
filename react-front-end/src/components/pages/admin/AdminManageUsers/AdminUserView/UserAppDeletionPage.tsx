import { ApplicationInfoRow } from "../../../../../api/api-types"

const UserAppDeletionPage = ({ appInfo, userUid, }: {
  appInfo: ApplicationInfoRow,
  userUid: number,
}) => {
  return <></>  
}

export default UserAppDeletionPage
